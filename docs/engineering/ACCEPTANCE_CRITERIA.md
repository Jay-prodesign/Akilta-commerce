# Acceptance Criteria — Reality Gate Evidence Mapping

This document maps each Reality Gate (RG-*) to the executable evidence it requires. Listing a gate here does not mark it PASS; a gate is only PASS when the mapped evidence has actually been produced and captured in the repository (CI run, test output, audit output, etc.).

| Gate | Maps to invariant | Required evidence (when applicable) | Current status |
|---|---|---|---|
| RG-OUTBOX | I1 — External effect / durable outbox | Outbox table/queue implementation, transactional write test, replay/at-least-once delivery test | **IMPLEMENTED** (Job/Run scope), Brain PASS at `b456466` (VER-005) — `packages/events/src/outbox.ts`, `migrations/0002_job_run_outbox_quota.sql` (`job_outbox`), `tests/contract/job-outbox.spec.ts` OUTBOX-01/03/04/05, plus real local-PostgreSQL transactional commit/rollback + `ON CONFLICT DO NOTHING` replay-safety proof (F3 remediation, `scripts/pg-remediation-evidence.mjs`, `docs/exec-plans/active/P0-023-remediation-F1-F2-F3.md`) |
| RG-DUP | I2 — Idempotency | Idempotency-key test covering MerchantWorkspace + integration/provider account + operation + action scope | PARTIAL — pre-existing `operational_events`/`action_executions` DB-level coverage (see `tests/contract/schema-migration.spec.mjs` `DB-03-IDEMPOTENCY-UNIQUE`) plus new Job-level app + DB dedup: `packages/events/src/job.ts` `jobDeduplicationKey`/`classifyJobSubmission`, `jobs` `UNIQUE` constraint, `tests/contract/job-outbox.spec.ts` JOB-08/09 |
| RG-SCOPE | I4 — Isolation | Tenant/workspace/integration isolation test, fail-closed-on-missing-scope test | Not yet implemented (as a dedicated RG-SCOPE suite); tenant-scope checks exist ad hoc in `action-engine.spec.ts` (`CAPABILITY-WRONG-WORKSPACE-DENY` etc.) and in the new Job/outbox/quota tests (`JOB-05`, `OUTBOX-04`, `QUOTA-04`, `QUOTA-08`) |
| RG-AUTH-RACE | I3 — Authority revalidation | Test proving revalidation of permission/approval/entitlement/kill-switch immediately before a queued or retried external effect | **IMPLEMENTED** — `apps/api/src/dispatch-safety.ts` (`revalidateBeforeDispatch`), `tests/security/dispatch-safety.spec.ts` DISPATCH-01..06. Single-owner lease/fencing for Job/Run dispatch (the same "queued or retried external effect" boundary) is also **IMPLEMENTED** under this gate, Brain PASS at `b456466` (VER-005) — `packages/events/src/job-lease.ts` (`acquireJobLease`, `evaluateFencedOperation`), `migrations/0003_job_lease_quota_budget.sql` (`jobs.owner_lease_id`/`lease_expires_at`/`fencing_token`), `tests/contract/job-lease.spec.ts` (6 scenarios) plus real concurrent-PostgreSQL proof (8 genuinely concurrent client connections race for one lease, exactly 1 wins) in F1 remediation |
| RG-LIFECYCLE | Engineer/Brain state ceiling | Test/process evidence that engineer output cannot self-promote past `IMPLEMENTED` | Not yet implemented |
| RG-LEARNING | I7 — Governed learning | Test proving Candidate/Observation/Evaluation/Superseded/Revoked/unapproved learning is mechanically excluded from ACTIVE retrieval | Not yet implemented |
| RG-QUOTA | I5 — Quota/concurrency | Atomic quota/cost reservation test, bounded concurrency test, tenant fairness test | **IMPLEMENTED**, Brain PASS at `b456466` (VER-005) — `packages/events/src/quota-ledger.ts` (`AtomicQuotaLedger`, `boundedConcurrencyRun`), `migrations/0002_job_run_outbox_quota.sql` (`quota_reservations`), `tests/contract/quota-ledger.spec.ts` QUOTA-01..08, plus a real DB-enforced running-budget ceiling (`migrations/0003_job_lease_quota_budget.sql` `quota_budgets`, `CHECK (used_amount <= limit_amount)`) proven under 10 genuinely concurrent PostgreSQL client connections — exactly 5 granted against `limit_amount=5`, tenant isolation confirmed (F2 remediation) |
| RG-AKILTA-CONTRACT | I6 — AKILTA boundary | Authenticated, anti-replay, versioned cross-project contract test; AI Commerce re-authorization test | Not yet implemented (deferred-by-activation until a real cross-project transport exists) |

## Known current D-090 reality

- **I1 (transactional outbox):** IMPLEMENTED for the Job/Run surface (P0-026 + RUN-1 adversarial delta), and further proven under real PostgreSQL transactional commit/rollback + replay-safety (F3 remediation, Brain PASS at `b456466`). Not yet extended to every other external-effect boundary in the repository (`operational_events`/`action_executions` predate this and use their own DB-level idempotency, not this outbox).
- **I5 (atomic quota/cost reservation + concurrency/fairness):** IMPLEMENTED for the Job/Run surface, and further proven under a real DB-enforced running-budget ceiling exercised by 10 genuinely concurrent PostgreSQL connections with no overspend and confirmed tenant isolation (F2 remediation, Brain PASS at `b456466`).
- **I3 (authority revalidation immediately before dispatch/retry):** IMPLEMENTED via `dispatch-safety.ts`, reusing `action-engine.ts`'s existing authority/approval/kill-switch composition unchanged. Single-owner lease/fencing for Job/Run dispatch is also IMPLEMENTED under this gate (F1 remediation, Brain PASS at `b456466`): `packages/events/src/job-lease.ts` gives crash/restart/orphan reconciliation (an expired/unclaimed lease is always reclaimable) and stale/dual-worker rejection (a token below the current fencing floor is refused), proven under 8 genuinely concurrent PostgreSQL client connections racing for one lease (exactly 1 wins).
- All other applicable invariants remain PARTIAL or deferred, per existing authority, pending admitted product source and further Brain-directed implementation work.
- **Historical note (superseded):** an earlier version of this document classified "stale/dual worker rejection" and "double-resume protection" as NOT_APPLICABLE, reasoning that no lease/fencing or WAITING/RESUME concept existed anywhere in source. Brain's `db5e81b` audit (`CHANGES_REQUIRED`) corrected this: repo-absence of a safety concept is not valid grounds for N/A when canonical P0-026 acceptance requires that property for Job/Run + queued/retried external-effect execution. Lease/fencing was implemented and Brain-verified PASS as described above. WAITING/ANSWER/RESUME (double-resume) correctly remains NOT_APPLICABLE — no actual async human/reviewer wait state exists in frozen CR-V1's Job/Run path — and Brain's audit explicitly accepted that narrower basis. See `docs/exec-plans/active/P0-023-remediation-F1-F2-F3.md` for the full remediation record.

This repository bootstrap (RB-08) did not implement any of the above; P0-026, its RUN-1 adversarial delta, and the F1/F2/F3 remediation are the first implementations, scoped to the Job/Run/outbox/quota/lease/dispatch-safety surface only.
